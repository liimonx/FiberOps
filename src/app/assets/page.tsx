import { PageStub } from "@/components/PageStub";

export default function AssetsPage() {
  return (
    <PageStub
      title="Assets"
      description="Asset table + detail view with maintenance timeline, connection graph, photos, and status logs."
    />
  );
}

