import { PageStub } from "@/components/PageStub";

export default function SettingsPage() {
  return <PageStub title="Settings" description="Preferences, roles, notifications, and integrations (v1 scaffold)." />;
}

