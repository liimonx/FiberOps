import { PageStub } from "@/components/PageStub";

export default function NetworkMapPage() {
  return (
    <PageStub
      title="Network Map"
      description="Full-viewport GIS canvas with layers, search, route tracing, heatmap, measurement, and inspector (Mapbox + mock GeoJSON)."
    />
  );
}

