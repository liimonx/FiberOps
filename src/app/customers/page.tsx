import { PageStub } from "@/components/PageStub";

export default function CustomersPage() {
  return (
    <PageStub
      title="Customers"
      description="Searchable table + customer profile with signal health, connection path, billing badge, and incident history."
    />
  );
}

