import { PageStub } from "@/components/PageStub";

export default function DashboardPage() {
  return (
    <PageStub
      title="Dashboard"
      description="KPIs, outage feed, technician widget, charts, mini-map, and work-order summary (mock-driven)."
    />
  );
}

