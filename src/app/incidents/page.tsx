import { PageStub } from "@/components/PageStub";

export default function IncidentsPage() {
  return (
    <PageStub
      title="Incidents"
      description="Incident list with severity filters, map pinpoints, assignments, timeline, and resolution notes."
    />
  );
}

